namespace Adapter
{
    /// <summary>
    /// The interface that implements the class is necessary for the client but not compatible with it.
    /// </summary>
    public interface IPiano
    {
        void PlayThePiano();
    }
}